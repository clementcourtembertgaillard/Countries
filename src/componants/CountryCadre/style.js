import { makeStyles } from "@mui/styles";

const useStyle = makeStyles(() => ({
  cardWrapper: {
    maxWidth: "10%",
    minHeight: "300",
    maxHeight: "300",
    margin: "0"
  }
}));

export default useStyle;